function los = detect_los(edge, box)
    % detect if the line segment (edge) intersect with the box (obstacle)
    % return true  --> line-of-sight
    %        false --> none-line-of-sight
    clip = clipEdge3d(an1_tag_edge, box1);

    if sum(isnan(clip)) ==6
        fprintf("los\n")
    else
        fprintf("nlos\n")
    end
    
end