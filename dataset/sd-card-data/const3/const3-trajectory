(1) const3-log1 (TDOA2)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(2) const3-log2 (TDOA2)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(3) const3-log3 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
               
(4) const3-log4 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.5 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
                     
(5) const3-log5 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(6) const3-log6 (TDOA2)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(7) const-log7 (TDOA2)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
# ------------------------------------------------------------------------- #

(8) const3-log8 (TDOA3)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(9) const3-log9 (TDOA3)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(10) const3-log10 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(11) const3-log11 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.5 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(12) const3-log12 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5        
        
(13) const3-log13 (TDOA3)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5          
        
(14) const3-log14 (TDOA3)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5    
        
# -------------------------- TWR ---------------------------------- #
(15) const3-log15 (TWR)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5   
        
(16) const3-log16 (TWR)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5          
        
(17) const3-log17 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(18) const3-log18 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.5 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(19) const3-log19 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(20) const3-log20 (TWR)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(21) const3-log21 (TWR)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
        
        
        
        
        
               
        
        
        
         
