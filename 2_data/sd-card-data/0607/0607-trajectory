(1) cfBolt-log00 (TDOA2)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(2) cfBolt-log01 (TDOA2)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(3) cfBolt-log02 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
               
(4) cfBolt-log03 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.5 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
                     
(5) cfBolt-log04 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(6) cfBolt-log05 (TDOA2)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(7) cfBolt-log06 (TDOA2)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(8) cfBolt-log07 (TDOA3)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(9) cfBolt-log08 (TDOA3)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(10) cfBolt-log09 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(11) cfBolt-log10 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.5 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5
        
(12) cfBolt-log11 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5        
        
(13) cfBolt-log12 (TDOA3)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5          
        
(14) cfBolt-log13 (TDOA3)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5    
        
# -------------------------- TWR ---------------------------------- #
(15) cfBolt-log14 (TWR)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5   
        
(16) cfBolt-log15 (TWR)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.45)
        HEIGHT = 1.5          
        
(17) cfBolt-log16 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(18) cfBolt-log17 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.5 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.5 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(19) cfBolt-log18 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(20) cfBolt-log19 (TWR)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
(21) cfBolt-log20 (TWR)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.75 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.5, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
        
        
        
        
        
        
               
        
        
        
         
