(1) cfBolt-0612-log1 (TDOA2)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.3)
        HEIGHT = 1.5
  
(2) cfBolt-0612-log2 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.35 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
  
(3) cfBolt-0612-log3 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.25 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.35 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5
                
(4) cfBolt-0612-log4 (TDOA2)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(5) cfBolt-0612-log5 (TDOA2)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(6) cfBolt-0612-log6 (TDOA2)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

# -------------------------------------------------------------------------------- #

(7) cfBolt-0612-log7 (TDOA3)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5


(8) cfBolt-0612-log8 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.35 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5


(9) cfBolt-0612-log9 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.25 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.35 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5


(10) cfBolt-0612-log10 (TDOA3)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5


(11) cfBolt-0612-log11 (TDOA3)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(12) cfBolt-0612-log12 (TDOA3)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5


# ---------------------------------------------------------------------------------- #

(13) cfBolt-0612-log13 (TWR)
        cmd_x = radius * np.cos(omega * t)
        cmd_y = radius * np.sin(omega * t)
        cmd_z = HEIGHT 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(14) cfBolt-0612-log14 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = -1 * radius * np.sin(0.75 * omega * t) * np.cos(0.15 * omega * t)
        cmd_z = HEIGHT - 0.35 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(15) cfBolt-0612-log15 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = 1.25 * radius * np.sin(0.65 * omega * t) * np.cos(0.85 * omega * t)
        cmd_z = HEIGHT - 0.35 * np.sin(0.75 * omega * t) 
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(16) cfBolt-0612-log16 (TWR)
        cmd_x = radius * np.cos(0.65 * omega * t)
        cmd_y = radius * np.sin(0.45 * omega * t) 
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(17) cfBolt-0612-log17 (TWR)
        cmd_x = radius * np.cos(1.15 * omega * t)
        cmd_y = radius * np.sin(0.75 * omega * t) * np.cos(0.6 * omega * t) 
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.7 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5

(18) cfBolt-0612-log18 (TWR)
        cmd_x = radius * np.cos(0.85 * omega * t)
        cmd_y = radius * np.sin(1.15 * omega * t)  
        cmd_z = HEIGHT - 0.45 * np.sin(0.75 * omega * t) * np.cos(0.65 * omega * t)
        
        goCircle(timeHelper, cf, radius=1.25, round_num=6.0, omega=0.35)
        HEIGHT = 1.5











